#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int a, n; cin >> n;
    vector<int> v;
    while (n--) { cin >> a; v.emplace_back(a); }
    for (int i = 0; i < *max_element(v); i++) {
                
    } 
}