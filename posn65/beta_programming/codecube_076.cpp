#include <bits/stdc++.h>
using namespace std;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    long double a, b, c; cin >> a >> b >> c;
    (a + b == c) ? cout << "Correct" : cout << "Wrong";
}