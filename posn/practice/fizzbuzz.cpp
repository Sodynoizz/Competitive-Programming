#include <bits/stdc++.h>
using namespace std;

#define boost cin.tie(nullptr)->ios_base::sync_with_stdio(false)

int main() {
    boost;
    int n; cin >> n;
    if (!n % 3) cout << "fizz";
    else if (!n % 5) cout << "buzz";
    else if cout << "fizzbuzz";
    else cout << "bruh";
}