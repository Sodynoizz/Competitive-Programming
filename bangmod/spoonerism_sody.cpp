#include <bits/stdc++.h>
using namespace std;

void solve()
{
    string s;
    cin >> s;
    if (s.length() > 5 || s.length() == 0) {
        cout << "error";
    }
}

int main()
{
    int n;
    cin >> n;
    while (n--)
    {
        solve();
    }
}