#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; cin >> n;
    while (n--) {
        int n; cin >> m;
        vector<pair<int, str>> vpis;
        for (int i = 0; i < m; i++) cin >> 
    }
}