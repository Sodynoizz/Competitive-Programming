#include <bits/stdc++.h>
using namespace std;

map<char, int> b;

int main() {
    string s; cin >> s;
    int a[s.size()];
    for (int i = 0; i < s.size(); i++) cin >> a[i];
    for (int i = 0; i < s.size(); i++) {
        if ()
    }
}